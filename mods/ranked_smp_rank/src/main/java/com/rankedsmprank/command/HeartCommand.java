package com.rankedsmprank.command;

import com.mojang.brigadier.CommandDispatcher;
import net.minecraft.server.command.ServerCommandSource;

/** Retained as stub — /heart space clean and /heart space reset have been removed. */
public final class HeartCommand {

    private HeartCommand() {}

    public static void register(CommandDispatcher<ServerCommandSource> dispatcher) {
        // Commands removed: /heart space clean, /heart space reset
    }
}
